apiVersion: v1
kind: Service
metadata:
  name: taskly-backend-service
spec:
  type: NodePort
  selector:
    app: taskly-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080